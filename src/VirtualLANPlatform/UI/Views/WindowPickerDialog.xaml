<Window x:Class="VirtualLANPlatform.UI.Views.WindowPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VirtualLANPlatform.UI.Localization"
        Title="{loc:Tr Share_Tab}"
        FlowDirection="{Binding Flow, Source={x:Static loc:Loc.I}}"
        FontFamily="{Binding Font, Source={x:Static loc:Loc.I}}"
        Width="740" SizeToContent="Height"
        MinHeight="320" MaxHeight="600"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Border Background="#2B2D31" CornerRadius="12"
            BorderBrush="#1E1F22" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="180" MaxHeight="420"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="20,16,20,10">
                <TextBlock Text="{loc:Tr Wp_Title}"
                           FontSize="15" FontWeight="SemiBold"
                           Foreground="#DBDEE1"/>
                <TextBlock Text="{loc:Tr Wp_Subtitle}"
                           FontSize="12" Foreground="#72767D" Margin="0,3,0,0"/>
            </StackPanel>

            <!-- Window cards -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="12,0">
                <WrapPanel x:Name="WindowsPanel" Orientation="Horizontal"/>
            </ScrollViewer>

            <!-- Audio -->
            <Border Grid.Row="2" Margin="16,10,16,4"
                    Background="#1E1F22" CornerRadius="8" Padding="14,10">
                <CheckBox x:Name="AudioCheck"
                          Content="{loc:Tr Wp_SystemAudio}"
                          Foreground="#B9BBBE" FontSize="13" Cursor="Hand">
                    <CheckBox.Template>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border x:Name="Box" Width="18" Height="18" CornerRadius="4"
                                        BorderBrush="#4F5660" BorderThickness="2"
                                        Background="Transparent" Margin="0,0,8,0">
                                    <TextBlock x:Name="Tick" Text="✓" FontSize="12"
                                               Foreground="White" HorizontalAlignment="Center"
                                               VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box"  Property="Background"    Value="#4361EE"/>
                                    <Setter TargetName="Box"  Property="BorderBrush"   Value="#4361EE"/>
                                    <Setter TargetName="Tick" Property="Visibility"    Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush" Value="#7B9EFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </CheckBox.Template>
                </CheckBox>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Margin="16,8,16,16">
                <Button x:Name="OkBtn" Content="{loc:Tr Wp_StartBtn}" IsDefault="True"
                        Click="Ok_Click" Cursor="Hand"
                        Background="#4361EE" Foreground="White"
                        BorderThickness="0" Padding="18,9" FontSize="13" Margin="0,0,8,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#3650D8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="{loc:Tr Common_Cancel}" IsCancel="True" Click="Cancel_Click" Cursor="Hand"
                        Background="#313338" Foreground="#B9BBBE"
                        BorderThickness="0" Padding="18,9" FontSize="13">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#3C3F45"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
